# 引き継ぎ書A: Mac mini (Batou) 現地・監査フェーズ 2026-07-14

あなたはMac mini(Batou/niihama)上で起動されたローカルエージェント(Claude CodeまたはCodex)。
オーナーのジョニーが現地に同席している。司令部はクラウドのClaudeセッション(ジョニーのiPhoneのClaudeアプリ)。

**この引き継ぎ書Aの範囲は「監査のみ」。システムへの変更はゼロ。** 変更作業(Tailscale一本化など)は、
監査データを見た司令部がコマンド・影響・rollbackまで固定した「引き継ぎ書B」を別途発行し、
ジョニーとGPT(sol)のレビューを経てから行う。Bが届くまで一切の変更をしないこと。

## 絶対規則(A フェーズ)

1. システム状態の変更禁止: 設定・サービス・ファイルの作成/変更/削除、launchctl、インストール、
   `pmset`・`systemsetup` の書き込み、reboot、OSアップデート — すべて禁止
2. ネットワーク送信禁止: git push、外部アップロード、いかなる自動送信もしない。
   例外はレポート1ファイルを ~/Desktop に保存することのみ
3. $HOMEの全走査禁止: 調査対象は監査スクリプトに明示列挙されたパスのみ
4. sudoが必要な項目は実行せずスキップし「要sudo」と記録
5. s9への一切のアクセス・変更禁止
6. 迷ったらジョニーに聞く(隣にいる)

## 手順

1. 監査スクリプトを実行する。改竄防止のためコミット固定URLから取得し、SHA-256を照合してから実行:
   (URLと期待ハッシュは司令部がジョニーに直接渡す。ハッシュ不一致なら実行せず報告)
2. レポート(~/Desktop/mini-audit-*.md)をジョニーと一緒に画面で確認する
3. ジョニーが内容を確認・了承したら、ジョニー自身がiPhoneのClaudeアプリへ貼って司令部に渡す
   (人間の目を通すことが外部送信のpreflightを兼ねる)
4. **ここで停止。** 司令部からの引き継ぎ書B(コミット固定・レビュー済み)を待つ

## 文脈(要点)

- 今朝、現母艦s9が電源断で死亡→復旧。教訓「ソフト起因で物理対応が必要になる状態を作らない」
- 方針: このminiをgreenfieldで新母艦化。s9は現用のまま。レガシーGITSの丸ごと移行はしない
- 旧Codex移行プログラム(Architecture B)は凍結中。凍結条件(reboot/OS更新/cutover/s9変更の禁止)を尊重
- 旧監査(2026-07-14以前)の主要指摘: Tailscale 3系統共存、rootプロセスがUID501書換可能な
  Homebrewバイナリを実行の疑い、FileVault OFF、未承認LaunchAgent 12件 — 今回のAフェーズで現状を再確認する

## 参照

- 復旧記録: https://github.com/ikedhidenori/jp/pull/4
- 計画: Linear JON-76 / JON-75
